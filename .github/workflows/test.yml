name: Baseline iOS Build test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Xcode Version
        run: xcodebuild -version

      - name: Show Resolved Build Settings
        run: |
          xcodebuild \
            -project ChatGPT.xcodeproj \
            -scheme ChatGPT \
            -configuration Release \
            -showBuildSettings | \
            grep -E 'IPHONEOS_DEPLOYMENT_TARGET|ARCHS|VALID_ARCHS|SDKROOT'

      - name: Build
        run: |
          xcodebuild \
            -project ChatGPT.xcodeproj \
            -scheme ChatGPT \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            ONLY_ACTIVE_ARCH=NO \
            clean build
